@model List<Category>

@{
    ViewBag.Title = "Categories";
    Layout = "_Layout";
}

<h2>title</h2>

@foreach (var item in Model)
{
    <p>
        @item.Name
        <a asp-action="CategoryArticles" asp-route-categoryId="@item.CategoryId">link na kategoriju</a>
    </p>
}
